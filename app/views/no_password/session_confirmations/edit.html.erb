<div class="h-screen bg-gray-50">
  <div class="min-h-full flex flex-col justify-center py-12 sm:px-6 lg:px-8">
    <div class="sm:mx-auto sm:w-full sm:max-w-md">
      <%= image_tag("no_password/creditario.svg", class:"mx-auto h-12 w-auto", alt: "Workflow")%>
      <h2 class="mt-6 text-center text-3xl font-extrabold text-skin-base">
        <%= t("no_password.session_confirmations.edit.title") %>
      </h2>
    </div>
    <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
      <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
        <%= form_with model: @resource, local: true, url: no_password.session_confirmations_path, method: "patch", class: "space-y-6" do |form| %>
          <p class="text-skin-muted px-4 text-center">
            <%= t("no_password.session_confirmations.edit.description") %>
          </p>
          <div>
            <label class="block text-sm font-medium text-skin-muted"><%= form.label :código %></label>
            <div class="mt-1">
              <%= form.text_field :token, maxlength: 60, required: true, class: "input w-full", type: "text", placeholder: t("form.placeholders.token") %>
            </div>
          </div>
          <div>
            <%= form.submit t("form.submit.login"), class: "w-full flex button button-accented" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
